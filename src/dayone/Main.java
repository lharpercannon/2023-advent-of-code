package dayone;

import java.io.FileNotFoundException;
import java.io.FileReader;
import java.util.Scanner;

public class Main {

    public static void main(String[] args) throws FileNotFoundException {

        // Read in contents of text file, split at the line break and put each line into an array


        // Create a hashmap with key value pairs of numbers one through ten spelled out (keys) and
        // numeric versions (values)

        // use a loop/string api to convert each item in the array into lowercase

        // Use substring , regex, whatever to look from the left and find the first number and look from
        // the right and find the first number


    }
}
